apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-rest-api.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.image.containerPort }}
  selector:
    app: {{ include "my-rest-api.name" . }}
